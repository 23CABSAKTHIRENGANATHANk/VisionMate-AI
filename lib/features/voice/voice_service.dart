// Placeholder service for future voice guidance features.
class VoiceService {
  VoiceService._();

  static final VoiceService instance = VoiceService._();

  void speak(String text) {
    // Voice guidance will be implemented in a future iteration.
    assert(text.isNotEmpty);
  }
}
